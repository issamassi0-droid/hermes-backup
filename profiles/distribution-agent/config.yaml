# distribution-agent — Tier B (distribution / publish worker)
# Inherits all defaults from config.base.yaml; overrides below.

model:
  default: nemotron-3-ultra-free
  provider: opencode-free
  base_url: https://opencode.ai/zen/v1

agent:
  max_turns: 50
  reasoning_effort: low

browser:
  enabled: true

display:
  skin: default

skills:
  enabled:
    - obsidian-cli
    - publish-to-obsidian
    - docx
    - pdf
    - humanizer
    - deep-web-research
    - browser
    - session_search
    - web
    - code_execution
    - terminal
    - delegation
    - file
    - image_gen
    - memory
    - skills
    - todo
    - tts
    - vision
    - clarify
    - computer_use
    - cronjob

plugins:
  enabled:
    - deer-flow
    - open-second-brain
    - superpowers
